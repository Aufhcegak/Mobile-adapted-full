# 手机版“点按 vs 滑动”交互交接文档

> 日期：2026-08-22
> 适用：手机版星露谷 1.6.15 + SMAPI-Android，直接把 PC 模组迁到手机后出现的列表/滚动交互问题。

---

## 1. 背景

PC 模组的列表交互通常依赖：

- 鼠标滚轮：`receiveScrollWheelAction()`
- 鼠标左键：`receiveLeftClick()` 按下即触发
- 原始滚动条 / SocialPage 的档案页跳转
- Shift / 右键 / F1

手机版没有滚轮、没有 Shift、没有鼠标右键；触摸滑动会被当成一次“点击”，导致：

- 想滑动翻页 → 变成点某一行
- 点某一行 → 可能没反应，或者跳到原版好感度/档案页
- 只有 `receiveScrollWheelAction` 的列表完全没法翻页

---

## 2. 统一修复模式

### 2.1 核心原则

1. **不要在 `receiveLeftClick` 里立刻执行“点行”动作**
   手机的一次触摸会先触发 `receiveLeftClick`，再触发 `leftClickHeld`，最后触发 `releaseLeftClick`。
   要区分“点按”和“滑动”，必须把动作推迟到 `releaseLeftClick`。

2. **在 `leftClickHeld` 里记录是否发生位移**
   位移超过阈值就标记为滑动；松手时：
   - 有位移 → 翻页，不执行点行
   - 没有位移 → 执行原来的点行逻辑

3. **不要依赖原版 SocialPage / 原生滚动条**
   原版 SocialPage 的右侧滚动条在手机上容易误触，且会把点击漏给原版“好感度档案页”。
   建议改为自绘分页列表。

4. **保留可点击的翻页按钮，同时支持滑动**
   滑动是主要操作，按钮作为兜底；两者都可用。

### 2.2 可直接复制的代码骨架

```csharp
// 字段
private bool _dragging;
private int _pressX;
private int _pressY;
private int _pendingRow = -1;

// 按下：只记录，不立即执行
public override void receiveLeftClick(int x, int y, bool playSound = true)
{
    // 先处理关闭/翻页按钮，这些可以立即执行
    if (_backBtn.Contains(x, y)) { /* 返回 */ return; }
    if (_upBtn.Contains(x, y)) { GoPrev(); return; }
    if (_downBtn.Contains(x, y)) { GoNext(); return; }

    _pressX = x;
    _pressY = y;
    _dragging = false;
    _pendingRow = FindRowAt(x, y); // 记录按在哪一行，不打开
}

// 拖动中：判断是否变成滑动
public override void leftClickHeld(int x, int y)
{
    base.leftClickHeld(x, y);
    if (_pendingRow < 0) return;
    if (Math.Abs(y - _pressY) > FontFit.Px(20) ||
        Math.Abs(x - _pressX) > FontFit.Px(20))
    {
        _dragging = true;
    }
}

// 松手：根据位移决定是“点按”还是“翻页”
public override void releaseLeftClick(int x, int y)
{
    base.releaseLeftClick(x, y);

    if (_pendingRow < 0) return;

    int row = _pendingRow;
    _pendingRow = -1;

    if (_dragging)
    {
        int dy = y - _pressY;
        if (dy < 0) GoNext();      // 往上滑 → 下一页
        else if (dy > 0) GoPrev(); // 往下滑 → 上一页
        _dragging = false;
        return;
    }

    if (row < 0 || row >= _rows.Count) return;
    // 这里执行原来的“点行”逻辑，例如打开送礼/详情/准备页面
    OpenRow(_rows[row]);
}
```

> 参考实现：`PhoneGift/PhoneMenu.cs`、`PhoneGift/BirthdayPrepMenu.cs`。
> 其中 `PhoneMenu` 已经彻底改成自绘列表，不再继承原版 `SocialPage`，因此不会出现“跳到好感度档案页”的问题。

---

## 3. 当前手机版模组中需要排查/修复的清单

### 3.1 已有滚轮翻页、大概率需要加滑动的模组

| 模组 | 文件 | 现状 | 需要做的 |
|---|---|---|---|
| MonsterArena | `MonsterInfoMenu.cs` | 掉落清单只有 `receiveScrollWheelAction`，手机上无法滚动 | 加“点按关闭”与“竖向滑动滚动”的区分；或加可视化增减按钮 |
| AnimalBarn | `HubMenu.cs` | 多个页签列表只有滚轮翻页 | 所有列表页签加滑动翻页 |
| AnimalBarn | `RoomSelectMenu.cs` | 有翻页按钮，但滑动可能仍被当成点击 | 加滑动翻页，保留按钮 |
| TreeFarm | `TreeFarmMenu.cs` | 管理/升级页有翻页按钮，也有滚轮 | 加滑动翻页，保留按钮 |
| ShedConsoleComputer | `ConsoleMenu.cs` / `ChestUiCore.cs` | 需要确认是否只有滚轮或按钮 | 按同一模式加滑动 + 按钮兜底 |
| GarbageRecycler | `MonitorMenu.cs` / `LandMachineMenu.cs` / `WaterMachineMenu.cs` / `UiKit.cs` | 已有长按 = 右键；需确认列表翻页是否依赖滚轮 | 按同一模式加滑动 |
| AutoEatLowHealth | `FoodPriorityMenu.cs` | 食物优先级列表，需确认是否只有点击/滚轮 | 按同一模式加滑动 + 点按区分 |

### 3.2 以后新增/移植模组时的检查项

- [ ] 列表是否只实现了 `receiveScrollWheelAction`？
- [ ] 是否仍继承原版 `SocialPage`？
- [ ] 是否在 `receiveLeftClick` 里立刻打开详情页，导致滑动也被当成点击？
- [ ] 是否缺少 `leftClickHeld` / `releaseLeftClick` 的“点按 vs 滑动”区分？
- [ ] 是否仍显示原版右侧滑动条？
- [ ] 是否保留了至少一种手机可用的翻页按钮 / 滑动提示？

---

## 4. 验证方式

1. 用 adb 连接手机，替换对应模组的 DLL。
2. 导入测试存档：`StardewSave_luguan_443163736.zip`
3. 逐个打开菜单验证：
   - 点一行 → 应正常进入详情/操作
   - 上下滑动 → 应翻页，不应误触某一行
   - 底部/侧边按钮 → 仍应能翻页
4. 抓日志：
   ```bash
   adb logcat | grep -i "SMAPI\|Exception\|PhoneGift\|MonsterArena\|TreeFarm"
   ```

---

## 5. 已完成的参考修复

- PhoneGift 送礼列表：自绘列表 + 隐藏右侧滑动条 + 点按送礼 + 上下滑动翻页 + `^/v` 按钮兜底。
- PhoneGift 生日列表 / 生日准备列表：同样支持“点按进入 + 上下滑动翻页”。
- 移动端包已更新：
  - `C:\Users\xiepe\Desktop\mobile mods\`
  - `C:\Users\xiepe\Desktop\Mobile-adapted-full\`
  - `压缩包\Phone Gift-1.1.0.zip`
